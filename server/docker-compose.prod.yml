services:
  redis:
    ports: !override []

  postgres:
    env_file: '.env.docker.local'
    ports: !override []
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U ${DB_USER} -d ${DB_NAME}']
      interval: 10s
      timeout: 5s
      retries: 5

  server:
    image: 'flashcard:latest'
    container_name: server
    restart: always
    env_file: '.env'
    ports:
      - 3000:3000
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started
        restart: true
